The back end the Rest-Server tests and gate serve: a `Login` and a handful of
services, each a Tonel file loaded on the first request for it.  `Scratch.class.st`
is written and removed by the tests themselves.
